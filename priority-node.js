class Node {
  constructor(value, priority) {
    this.value = value;
    this.priority = priority;
    this.next = null;
  }
}
// now each node has a value and a priority and can point to the next node in the tree
